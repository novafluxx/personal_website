@code {
    private bool isMenuOpen = false;

    private void ToggleMenu()
    {
        isMenuOpen = !isMenuOpen;
    }

    private void CloseMenu()
    {
        isMenuOpen = false;
    }

    [Parameter]
    public EventCallback ToggleTheme { get; set; }

    [Parameter]
    public string CurrentTheme { get; set; } = "light";
}

<div class="nav-container" data-bs-theme="@CurrentTheme">
    <div class="mobile-menu-toggle" @onclick="ToggleMenu">
        <i class="fas fa-bars"></i>
    </div>

    <div class="nav-links @(isMenuOpen ? "menu-open" : "")">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All" @onclick="CloseMenu">
            <i class="fas fa-home"></i> Home
        </NavLink>
        <NavLink class="nav-link" href="experience" @onclick="CloseMenu">
            <i class="fas fa-briefcase"></i> Experience
        </NavLink>
        <NavLink class="nav-link" href="aviation" @onclick="CloseMenu">
            <i class="fas fa-plane"></i> Aviation
        </NavLink>
        <NavLink class="nav-link" href="contact" @onclick="CloseMenu">
            <i class="fas fa-envelope"></i> Contact
        </NavLink>
        <a href="javascript:void(0)" class="nav-link theme-toggle" @onclick="ToggleTheme">
            @if (CurrentTheme == "dark")
            {
                <i class="fas fa-sun"></i>
            }
            else
            {
                <i class="fas fa-moon"></i>
            }
        </a>
    </div>
</div>
